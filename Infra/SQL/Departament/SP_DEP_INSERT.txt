CREATE OR ALTER PROCEDURE SP_DEP_INSERT
(
	@P_NAME VARCHAR(100),
	@P_RETURN_VALUE INTEGER OUTPUT
)
AS
BEGIN
	INSERT INTO DEPARTAMENT (DEP_NAME) VALUES( @P_NAME)

	SELECT @P_RETURN_VALUE = MAX(DEP_ID) 
	  FROM DEPARTAMENT;
END;