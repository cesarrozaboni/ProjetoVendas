CREATE OR ALTER PROCEDURE SP_DEP_UPDATE
(
	@P_ID INTEGER,
	@P_NAME VARCHAR(100),
	@P_RETURN_VALUE INTEGER OUTPUT
)
AS
BEGIN
	UPDATE DEPARTAMENT 
	   SET DEP_NAME = @P_NAME 
	 WHERE DEP_ID = @P_ID

	SET @P_RETURN_VALUE = @@ROWCOUNT;
END;