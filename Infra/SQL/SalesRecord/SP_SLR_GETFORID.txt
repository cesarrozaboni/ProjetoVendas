CREATE OR ALTER PROCEDURE SP_SLR_GETFORID
(
	@P_ID INTEGER
)
AS
BEGIN
	SELECT 	SALE_ID,
			SALE_DATE,
			SALE_AMOUNT,
			SALE_STATUS,
			SALE_SELLER_ID
	  FROM SALES_RECORD
	 WHERE SALE_ID = @P_ID;
END;