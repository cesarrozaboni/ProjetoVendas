CREATE OR ALTER PROCEDURE SP_SEL_GETFORID
(
	@P_ID INTEGER
)
AS
BEGIN
	SELECT 	SELLER_ID,
			SELLER_NAME,
			SELLER_EMAIL,
			SELLER_BIRTHDATE,
			SELLER_SALARY_BASE,
			SELLER_ID_DEPARTAMENT
	  FROM SELLER
	 WHERE SELLER_ID = @P_ID;
END;